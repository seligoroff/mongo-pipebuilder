"""
Integration tests for mongo-pipebuilder.

These tests require a running MongoDB instance and verify that
built pipelines actually work with real MongoDB.

To run integration tests:
    pytest tests/integration -m integration

To skip integration tests (default):
    pytest  # Only runs unit tests
"""



