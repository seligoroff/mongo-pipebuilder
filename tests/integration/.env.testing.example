# MongoDB Test Configuration
# Copy this file to .env.testing and adjust values as needed
# .env.testing is gitignored, so you can customize it locally

# MongoDB connection string
# Default: mongodb://localhost:27017/
MONGODB_TEST_URI=mongodb://localhost:27017/

# MongoDB connection timeout (milliseconds)
# Default: 2000
MONGODB_TEST_TIMEOUT=2000

# Test database name prefix (used for temporary databases)
# Default: test_pipebuilder (will append random suffix)
# Only used if MONGODB_TEST_DB_NAME is not set
MONGODB_TEST_DB_PREFIX=test_pipebuilder

# Optional: Fixed database name
# If set, this database will be used for all tests (database will NOT be dropped)
# If not set, temporary databases will be created and dropped after each test
# MONGODB_TEST_DB_NAME=my_test_db

# Optional: MongoDB authentication
# MONGODB_TEST_USERNAME=testuser
# MONGODB_TEST_PASSWORD=testpass

# Optional: MongoDB replica set
# MONGODB_TEST_REPLICA_SET=rs0
